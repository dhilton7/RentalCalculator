<div class="row">
	<table class="table">
		<thead>
			<tr>
				<th>Name</th>
				<th>Link</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			<% deal.links.each do |l| %>
			<tr>
				<td><%= l.name %></td>
				<td><%= l.url %></td>
				<td><a href="#" class='btn btn-danger btn-sm'>Delete</a><%#= link_to "Delete" %></td>
			</tr>
			<% end %>
		</tbody>
	</table>
</div>